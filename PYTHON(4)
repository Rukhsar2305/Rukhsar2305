//store the following word meanings in a python dictionary.
table:"a piece of furniture" , "lists of facts & figures"
cat : "a small animal"
dictionary = {
    "cat" : "a small animal",
    "table" : [" a piece of furniture" , " lists of facts & figures"]
}
print(dictionary)

//You are given a subjects for lists of students.Assume one classroom is required for 1 subject. how many classrooms are needed by all students .
subjects = {
  "python" , "java" , "c++" ,"python" , "javascript" , "java" ,
  "python" , "java" , "C++" , "c"
}
print(len(subjects))

//WAP to enter marks of 3 subjects from the user and store them in a dictionary. Start with
an empty dictionary & add one by one. Use subject name as key & marks as value.
marks = {}
x = int(input("enter phy :"))
marks.update({"phy" : x})
x = int(input("enter maths :"))
marks.update({"maths" : x})
x = int(input("enter chem :"))
marks.update({"chem" : x})
print(marks)

//Figure out a way to store 9 & 9.0 as separate values in the set.
(You can take help of built-in data types)
values = {
    ("float" , 9.0),
    ("int" , 9 )
}
print(values)
